# <원격-로컬 git 충돌 해결법>
### 1. 로컬 커밋 또는 로컬임시저장
```
// 로컬에 커밋
git add .
git commit -m "커밋 메세지"

// 로컬에 임시저장
git stash
```

### 2. 원격 끌어오기 + 내 로컬코드 합치기
```
git pull --rebase
```

### 3. 로컬의 코드 논리검증, 오류 수정

### 4. 원격으로 push
```
git push
```
